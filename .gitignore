# build artifacts
spike/defs/*.scsyndef
live/defs/*.scsyndef
__pycache__/
*.pyc

# python venv (TUI dependency, not committed)
.venv/

# saved patches are personal; share one deliberately with `git add -f`
patches/

# scratch
/tmp/
*.log
